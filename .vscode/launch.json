{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch AudioPluginHost",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/libs/juce/build/extras/AudioPluginHost/AudioPluginHost_artefacts/AudioPluginHost",
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "preLaunchTask": "buildAndLaunch"
        }
    ]
}